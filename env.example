# Database Configuration
# ============================================
# MongoDB Connection String
# ============================================

# Для Vercel MongoDB (автоматически создается Vercel при подключении базы данных):
# MONGODB_URI создается автоматически Vercel, не нужно добавлять вручную!
# Формат: mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority

# Для локальной разработки используйте:
# MONGODB_URI=mongodb://localhost:27017/user_service
# Или для MongoDB Atlas:
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=24h

# Server
PORT=3000
NODE_ENV=development

