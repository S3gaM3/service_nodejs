# Инструкция по деплою на Vercel

Подробная пошаговая инструкция по развертыванию проекта на Vercel.

## Подготовка

### 1. Залить проект на GitHub

Если проект еще не на GitHub:

```bash
git init
git add .
git commit -m "Initial commit: User service ready for deployment"
git remote add origin https://github.com/ваш-username/ваш-repo-name.git
git branch -M main
git push -u origin main
```

### 2. Убедитесь, что все работает локально

Перед деплоем проверьте, что проект запускается:

```bash
npm run build:all
```

Если сборка прошла успешно, можно деплоить.

## Деплой Backend на Vercel

### Шаг 1: Создание проекта в Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New..."** → **"Project"**
4. Найдите и выберите ваш репозиторий
5. Нажмите **"Import"**

### Шаг 2: Настройка проекта

Vercel автоматически определит настройки из `vercel.json`, но проверьте:

- **Framework Preset:** Other (или оставьте пустым)
- **Root Directory:** `./` (корень проекта)
- **Build Command:** `npm run vercel-build` (уже настроено в vercel.json)
- **Output Directory:** оставьте пустым
- **Install Command:** `npm install`

### Шаг 3: Настройка переменных окружения

**ВАЖНО:** Не нажимайте "Deploy" сразу! Сначала настройте переменные окружения.

В разделе **"Environment Variables"** добавьте следующие переменные:

#### Обязательные переменные:

```
MONGODB_URI=mongodb+srv://Vercel-Admin-testzadan:u0Tx40vR0WVzyo4k@testzadan.hxw1kff.mongodb.net/?retryWrites=true&w=majority
```

```
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа-для-jwt-токенов
```

```
JWT_EXPIRES_IN=24h
```

```
NODE_ENV=production
```

#### Как добавить переменные:

1. В настройках проекта найдите раздел **"Environment Variables"**
2. Нажмите **"Add"** для каждой переменной
3. Введите имя переменной (например, `MONGODB_URI`)
4. Введите значение (ваш MongoDB URI)
5. Выберите окружения: **Production**, **Preview**, **Development** (или только Production)
6. Нажмите **"Save"**

**Совет:** Для генерации безопасного `JWT_SECRET` используйте:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### Шаг 4: Деплой

1. После добавления всех переменных окружения нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 1-2 минуты)
3. После успешного деплоя вы получите URL вида: `https://your-project.vercel.app`

### Шаг 5: Проверка работы Backend

После деплоя проверьте, что API работает:

```bash
# Health check
curl https://your-project.vercel.app/health

# Должен вернуть:
# {"status":"ok","message":"Service is running","timestamp":"..."}
```

## Деплой Frontend на Vercel

### Вариант 1: Отдельный проект для Frontend (рекомендуется)

#### Шаг 1: Создание нового проекта

1. В Vercel нажмите **"Add New..."** → **"Project"**
2. Выберите тот же репозиторий
3. В настройках проекта укажите:
   - **Root Directory:** `client`
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`

#### Шаг 2: Настройка переменных окружения для Frontend

Добавьте переменную:

```
VITE_API_URL=https://your-backend-project.vercel.app/api
```

**Важно:** Замените `your-backend-project` на реальный URL вашего backend проекта.

#### Шаг 3: Деплой

Нажмите **"Deploy"** и дождитесь завершения.

### Вариант 2: Monorepo (оба проекта в одном репозитории)

Если хотите деплоить оба проекта из одного репозитория:

1. Создайте два проекта в Vercel, оба из одного репозитория
2. Для backend: Root Directory = `./`
3. Для frontend: Root Directory = `client`
4. Настройте переменные окружения для каждого проекта отдельно

## Проверка после деплоя

### Backend

1. Проверьте health endpoint:
   ```bash
   curl https://your-backend.vercel.app/health
   ```

2. Попробуйте зарегистрировать пользователя:
   ```bash
   curl -X POST https://your-backend.vercel.app/api/users/register \
     -H "Content-Type: application/json" \
     -d '{
       "fullName": "Test User",
       "dateOfBirth": "1990-01-01",
       "email": "test@example.com",
       "password": "password123"
     }'
   ```

### Frontend

1. Откройте URL вашего frontend проекта
2. Попробуйте войти с предустановленными данными:
   - Email: `admin@example.com`
   - Пароль: `password123`

## Инициализация базы данных на Production

После первого деплоя нужно создать предустановленных пользователей.

### Способ 1: Через API (рекомендуется)

Используйте предустановленные данные для регистрации администратора:

```bash
curl -X POST https://your-backend.vercel.app/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "fullName": "Администратор Системы",
    "dateOfBirth": "1985-01-15",
    "email": "admin@example.com",
    "password": "password123",
    "role": "admin"
  }'
```

### Способ 2: Включить автоматический seed

Если хотите, чтобы пользователи создавались автоматически при первом запуске:

1. В настройках проекта Vercel добавьте переменную:
   ```
   ENABLE_SEED=true
   ```

2. Перезадеплойте проект

**Внимание:** После создания пользователей рекомендуется удалить эту переменную или установить `ENABLE_SEED=false`, чтобы не создавать пользователей при каждом перезапуске.

## Настройка домена (опционально)

### Для Backend

1. В настройках проекта Vercel перейдите в **"Domains"**
2. Добавьте ваш домен (например, `api.yourdomain.com`)
3. Следуйте инструкциям для настройки DNS

### Для Frontend

1. Аналогично добавьте домен для frontend проекта
2. Обновите `VITE_API_URL` в переменных окружения на новый домен backend

## Автоматический деплой

После первого деплоя Vercel автоматически будет деплоить проект при каждом push в main ветку.

Для других веток создаются preview deployments, которые можно использовать для тестирования.

## Troubleshooting (Решение проблем)

### Ошибка подключения к MongoDB

**Проблема:** Backend не может подключиться к MongoDB

**Решение:**
1. Проверьте, что `MONGODB_URI` правильно добавлен в Environment Variables
2. Убедитесь, что в MongoDB Atlas разрешен доступ с IP адресов Vercel:
   - В MongoDB Atlas перейдите в **Network Access**
   - Добавьте `0.0.0.0/0` (разрешить со всех IP) или добавьте IP адреса Vercel
3. Проверьте логи в Vercel: **Settings** → **Logs**

### Ошибка сборки

**Проблема:** Сборка падает с ошибками TypeScript

**Решение:**
1. Проверьте, что проект собирается локально: `npm run build`
2. Убедитесь, что все зависимости указаны в `package.json`
3. Проверьте логи сборки в Vercel

### Frontend не может подключиться к Backend

**Проблема:** Frontend выдает ошибки при запросах к API

**Решение:**
1. Проверьте, что `VITE_API_URL` правильно настроен в Environment Variables frontend проекта
2. Убедитесь, что URL backend правильный (с `https://` и без `/api` в конце)
3. Проверьте CORS настройки на backend (должны разрешать запросы с frontend домена)

### Timeout ошибки

**Проблема:** Запросы занимают слишком много времени

**Решение:**
1. Проверьте оптимизацию запросов к БД
2. Убедитесь, что MongoDB находится в том же регионе, что и Vercel deployment
3. Рассмотрите использование connection pooling

## Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)
- [MongoDB Atlas Documentation](https://www.mongodb.com/docs/atlas/)

## Чек-лист перед деплоем

- [ ] Проект залит на GitHub
- [ ] Локальная сборка проходит успешно (`npm run build:all`)
- [ ] Создан проект в Vercel
- [ ] Добавлены все переменные окружения:
  - [ ] `MONGODB_URI`
  - [ ] `JWT_SECRET`
  - [ ] `JWT_EXPIRES_IN`
  - [ ] `NODE_ENV=production`
- [ ] Для frontend добавлен `VITE_API_URL`
- [ ] MongoDB Atlas настроен для доступа с Vercel IP
- [ ] Backend успешно задеплоен
- [ ] Frontend успешно задеплоен
- [ ] Health check endpoint работает
- [ ] Можно зарегистрировать/войти через API
- [ ] Frontend может подключиться к Backend

---

**Готово!** После выполнения всех шагов ваш проект будет доступен в интернете. 🚀

