# User Management Service

Веб-сервис для управления пользователями с полным функционалом: backend API на Node.js и современное клиентское приложение на React.

## 🌐 Работающий проект

Проект развернут и доступен для проверки:

**🔗 [https://service-nodejs-i933.vercel.app/](https://service-nodejs-i933.vercel.app/)**

### Для входа используйте:
- **Email:** `admin@example.com`
- **Пароль:** `password123`

Или любой из предустановленных пользователей (см. раздел "Предустановленные пользователи" ниже).

## Что это за проект?

Это полноценная система управления пользователями, которая включает:
- **Backend API** - REST API сервер с аутентификацией через JWT
- **Frontend приложение** - веб-интерфейс для работы с пользователями
- **База данных** - MongoDB для хранения данных пользователей

## Технологии

### Backend
- **Express** - популярный веб-фреймворк для Node.js
- **TypeScript** - для надежности и удобства разработки
- **MongoDB + Mongoose** - NoSQL база данных и удобная работа с ней
- **JWT** - безопасная аутентификация через токены
- **bcrypt** - надежное хеширование паролей
- **class-validator** - автоматическая валидация входящих данных

### Frontend
- **React 18** - современная библиотека для создания интерфейсов
- **TypeScript** - типизация для надежности кода
- **Vite** - быстрый сборщик и dev-сервер
- **React Router** - навигация между страницами
- **Axios** - удобная работа с API

## Структура проекта

Проект разделен на две части:

```
.
├── src/                    # Backend (серверная часть)
│   ├── config/            # Настройки (подключение к БД)
│   ├── controllers/       # Контроллеры (обработка запросов)
│   ├── dto/              # Валидация входных данных
│   ├── middleware/       # Промежуточное ПО (авторизация, обработка ошибок)
│   ├── models/          # Модели данных (схемы MongoDB)
│   ├── routes/          # Маршруты API
│   ├── services/        # Бизнес-логика
│   ├── utils/           # Вспомогательные функции
│   └── scripts/         # Скрипты (seed, очистка БД)
│
├── client/               # Frontend (клиентская часть)
│   ├── src/
│   │   ├── api/         # Клиент для работы с API
│   │   ├── components/  # React компоненты
│   │   ├── context/     # Управление состоянием (авторизация)
│   │   ├── pages/       # Страницы приложения
│   │   └── types/       # TypeScript типы
│   └── package.json
│
└── api/                  # Для деплоя на Vercel
```

## Быстрый старт

### 1. Установка

Установите все зависимости одной командой:

```bash
npm run install:all
```

Это установит зависимости и для backend, и для frontend.

### 2. Настройка переменных окружения

**Backend** - создайте файл `.env` в корне проекта:

```env
MONGODB_URI=mongodb://localhost:27017/user_service
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
JWT_EXPIRES_IN=24h
PORT=3000
NODE_ENV=development
```

**Frontend** - создайте файл `client/.env`:

```env
VITE_API_URL=http://localhost:3000/api
```

### 3. Запуск

Запустите весь проект одной командой:

```bash
npm run dev:all
```

После запуска:
- **Backend API** будет доступен на `http://localhost:3000`
- **Frontend приложение** откроется на `http://localhost:3001`

При первом запуске автоматически создадутся тестовые пользователи (см. раздел "Предустановленные пользователи" ниже).

## Команды проекта

### Основные команды

| Команда | Описание |
|---------|----------|
| `npm run install:all` | Установить все зависимости (backend + frontend) |
| `npm run dev:all` | Запустить backend и frontend одновременно |
| `npm run build:all` | Собрать весь проект для production |

### Команды для backend

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запустить backend в режиме разработки |
| `npm run dev:server` | То же самое, что `npm run dev` |
| `npm run build` | Собрать TypeScript в JavaScript |
| `npm start` | Запустить собранный backend |

### Команды для frontend

| Команда | Описание |
|---------|----------|
| `npm run dev:client` | Запустить frontend в режиме разработки |
| `cd client && npm run build` | Собрать frontend для production |

### Команды для работы с базой данных

| Команда | Описание |
|---------|----------|
| `npm run seed` | Создать тестовых пользователей (если БД пустая) |
| `npm run seed:force` | Очистить БД и создать пользователей заново |
| `npm run clear:db` | Удалить всех пользователей из БД |

## Предустановленные пользователи

При первом запуске автоматически создаются тестовые пользователи. Это удобно для быстрого старта и тестирования.

### Администратор

Для входа в систему как администратор:
- **Email:** `admin@example.com`
- **Пароль:** `password123`

Администратор имеет полный доступ ко всем функциям системы.

### Обычные пользователи

Создается 10 обычных пользователей, все с паролем `password123`:

1. `ivanov@example.com` - Иванов Иван Иванович
2. `petrova@example.com` - Петрова Мария Сергеевна
3. `sidorov@example.com` - Сидоров Петр Александрович
4. `kozlova@example.com` - Козлова Анна Дмитриевна
5. `morozov@example.com` - Морозов Дмитрий Викторович
6. `volkova@example.com` - Волкова Елена Николаевна
7. `novikov@example.com` - Новиков Алексей Игоревич
8. `fedorova@example.com` - Федорова Ольга Владимировна
9. `lebedev@example.com` - Лебедев Сергей Павлович
10. `sokolova@example.com` - Соколова Татьяна Андреевна

**Важно:** 
- Пароль для всех пользователей: `password123`
- Пользователи создаются автоматически только при первом запуске
- Если нужно пересоздать пользователей, используйте `npm run seed:force`

## API Endpoints

Backend предоставляет следующие endpoints:

### 1. Регистрация
**POST** `/api/users/register`

Создает нового пользователя. После регистрации автоматически возвращается JWT токен.

Пример запроса:
```json
{
  "fullName": "Иванов Иван Иванович",
  "dateOfBirth": "1990-01-01",
  "email": "user@example.com",
  "password": "password123",
  "role": "user"
}
```

### 2. Авторизация
**POST** `/api/users/login`

Вход в систему. Возвращает JWT токен, который нужно использовать для доступа к защищенным endpoints.

Пример запроса:
```json
{
  "email": "admin@example.com",
  "password": "password123"
}
```

### 3. Получить пользователя по ID
**GET** `/api/users/:id`

Получить информацию о конкретном пользователе. Может получить либо администратор (любого пользователя), либо пользователь сам себя.

Требует заголовок: `Authorization: Bearer <token>`

### 4. Список всех пользователей
**GET** `/api/users`

Получить список всех пользователей. **Только для администраторов.**

Требует заголовок: `Authorization: Bearer <token>`

### 5. Блокировка пользователя
**PATCH** `/api/users/:id/block`

Заблокировать пользователя. Может выполнить либо администратор, либо пользователь сам себя.

Требует заголовок: `Authorization: Bearer <token>`

## Клиентское приложение

Frontend предоставляет удобный веб-интерфейс для работы с системой.

### Доступные страницы

- **`/login`** - Страница входа
- **`/register`** - Регистрация нового пользователя
- **`/`** - Dashboard (главная страница после входа)
- **`/users`** - Список всех пользователей (только для админов)
- **`/users/:id`** - Детальная информация о пользователе

### Функциональность

- ✅ Автоматическое сохранение сессии (не нужно вводить пароль при каждом обновлении страницы)
- ✅ Защищенные маршруты (автоматический редирект на страницу входа, если не авторизован)
- ✅ Разные права доступа для админов и обычных пользователей
- ✅ Удобный интерфейс для управления пользователями

## Модель пользователя

Каждый пользователь в системе имеет следующие поля:

- **`_id`** - Уникальный идентификатор (ObjectId MongoDB)
- **`fullName`** - Полное имя (ФИО)
- **`dateOfBirth`** - Дата рождения
- **`email`** - Email адрес (уникальный, используется для входа)
- **`password`** - Пароль (хранится в захешированном виде)
- **`role`** - Роль: `admin` или `user`
- **`isActive`** - Статус: активен пользователь или заблокирован
- **`createdAt`** - Дата и время регистрации
- **`updatedAt`** - Дата и время последнего обновления

## Безопасность

В проекте реализованы следующие меры безопасности:

- **Хеширование паролей** - пароли никогда не хранятся в открытом виде, используется bcrypt
- **JWT токены** - безопасная аутентификация без хранения сессий на сервере
- **Валидация данных** - все входящие данные проверяются перед обработкой
- **Проверка прав доступа** - каждый endpoint проверяет, имеет ли пользователь право на выполнение действия
- **Обработка ошибок** - безопасные сообщения об ошибках (в production не показываются детали)
---

**Приятной работы с проектом!** 🚀
