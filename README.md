# User Service

Сервис для управления пользователями с аутентификацией и авторизацией.

## Технологии

- **Express** - веб-фреймворк
- **TypeScript** - типизированный JavaScript
- **MongoDB** - NoSQL база данных
- **Mongoose** - ODM для работы с MongoDB
- **@vercel/functions** - оптимизация для serverless окружения Vercel
- **JWT** - токены для аутентификации
- **bcrypt** - хеширование паролей
- **class-validator** - валидация данных

## Структура проекта

```
src/
├── config/          # Конфигурация (база данных)
├── controllers/     # Контроллеры
├── dto/            # Data Transfer Objects для валидации
├── middleware/     # Middleware (аутентификация, валидация, обработка ошибок)
├── models/         # Модели данных (Mongoose schemas)
├── routes/         # Маршруты
├── services/       # Бизнес-логика
├── types/          # TypeScript типы
├── utils/          # Утилиты
├── index.ts        # Экспорт app для Vercel
└── server.ts       # Точка входа для локальной разработки
api/
└── index.ts        # Serverless функция для Vercel
```

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

3. Настройте переменные окружения в `.env`:
   
   **Для MongoDB:**
   - `MONGODB_URI` - строка подключения к MongoDB
     - Для локальной разработки: `mongodb://localhost:27017/user_service`
     - Для MongoDB Atlas: `mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority`
     - Для Vercel MongoDB: создается автоматически при подключении базы данных
   
   **Общие:**
   - `JWT_SECRET` - секретный ключ для JWT
   - `JWT_EXPIRES_IN` - время жизни токена (по умолчанию: 24h)
   - `PORT` - порт сервера (по умолчанию: 3000)
   - `NODE_ENV` - окружение (development/production)
   
   **Примечание:** Для Vercel MongoDB переменная `MONGODB_URI` создается автоматически при подключении базы данных к проекту.

4. Убедитесь, что MongoDB запущен (для локальной разработки) или используйте MongoDB Atlas.

5. Запустите приложение:
```bash
# Режим разработки
npm run dev

# Продакшн
npm run build
npm start
```

## Деплой на Vercel

> 📖 **Подробная инструкция по деплою**: см. [DEPLOY.md](./DEPLOY.md)

### Быстрый старт

1. **Залить проект на GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-github-repo-url>
   git push -u origin main
   ```

2. **Подключить проект к Vercel**
   - Зайдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub
   - Нажмите "New Project"
   - Импортируйте ваш репозиторий
   - Vercel автоматически определит настройки из `vercel.json`

3. **Настроить MongoDB**
   
   **Создание базы данных MongoDB Atlas:**
   - В панели Vercel перейдите в раздел **Storage**
   - Найдите **MongoDB Atlas** в списке доступных баз данных
   - Нажмите **Create Database** или **Add Integration**
   - Выберите план (Free план доступен)
   - Выберите регион (рекомендуется тот же, что и для проекта)
   - Следуйте инструкциям для создания кластера MongoDB Atlas
   
   **Подключение базы данных к проекту:**
   - После создания базы данных откройте её настройки
   - Перейдите на вкладку **.env.local** или найдите кнопку **Connect**
   - Выберите проект, к которому нужно подключить базу данных
   - Vercel автоматически добавит переменную `MONGODB_URI` в настройки проекта
   - Переменная будет доступна во всех окружениях (Production, Preview, Development)
   
   **Проверка подключения:**
   - В настройках проекта перейдите в раздел **Environment Variables**
   - Убедитесь, что переменная `MONGODB_URI` присутствует
   - Формат: `mongodb+srv://user:password@cluster.mongodb.net/?retryWrites=true&w=majority`
   
   **Дополнительные переменные окружения:**
   
   В настройках проекта Vercel (Settings → Environment Variables) добавьте:
   
   ```
   JWT_SECRET=your-secret-key-min-32-chars
   JWT_EXPIRES_IN=24h
   NODE_ENV=production
   ```
   
   **Важно:** 
   - ✅ `MONGODB_URI` автоматически создается Vercel при подключении базы данных
   - ✅ Проект использует `@vercel/functions` для оптимизации соединений в serverless окружении
   - ✅ Соединения с MongoDB управляются автоматически через connection pooling
   - 📝 Для локальной разработки используйте `MONGODB_URI` с локальным MongoDB или MongoDB Atlas (см. `env.example`)

4. **Деплой**
   - После настройки переменных окружения Vercel автоматически задеплоит проект
   - Каждый push в main ветку будет автоматически деплоиться
   - URL будет доступен в панели Vercel

### Локальная разработка

Для локальной разработки используйте:
```bash
npm run dev
```

Это запустит сервер через `src/server.ts` с поддержкой hot-reload.

### Структура файлов для деплоя

- `src/index.ts` - экспортирует Express app для Vercel
- `src/server.ts` - точка входа для локальной разработки
- `api/index.ts` - serverless функция для Vercel
- `vercel.json` - конфигурация для Vercel
- `.vercelignore` - файлы, которые игнорируются при деплое

## API Endpoints

### 1. Регистрация пользователя
**POST** `/api/users/register`

Тело запроса:
```json
{
  "fullName": "Иванов Иван Иванович",
  "dateOfBirth": "1990-01-01",
  "email": "user@example.com",
  "password": "password123",
  "role": "user" // опционально, по умолчанию "user"
}
```

### 2. Авторизация пользователя
**POST** `/api/users/login`

Тело запроса:
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

Ответ содержит токен, который нужно использовать в заголовке `Authorization: Bearer <token>` для защищенных endpoints.

### 3. Получение пользователя по ID
**GET** `/api/users/:id`

Требует аутентификации. Может получить либо админ, либо пользователь сам себя.

### 4. Получение списка пользователей
**GET** `/api/users`

Требует аутентификации и прав администратора.

### 5. Блокировка пользователя
**PATCH** `/api/users/:id/block`

Требует аутентификации. Может заблокировать либо админ, либо пользователь сам себя.

## Модель пользователя

- `id` - UUID
- `fullName` - ФИО
- `dateOfBirth` - Дата рождения
- `email` - Email (уникальное значение)
- `password` - Пароль (хешированный)
- `role` - Роль (`admin` или `user`)
- `isActive` - Статус активности (активный/неактивный)
- `createdAt` - Дата создания
- `updatedAt` - Дата обновления

## Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для аутентификации
- Валидация входных данных
- Проверка прав доступа для каждого endpoint
- Обработка ошибок

## Примеры использования

### Регистрация
```bash
curl -X POST http://localhost:3000/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "fullName": "Иванов Иван Иванович",
    "dateOfBirth": "1990-01-01",
    "email": "ivan@example.com",
    "password": "password123"
  }'
```

### Авторизация
```bash
curl -X POST http://localhost:3000/api/users/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "ivan@example.com",
    "password": "password123"
  }'
```

### Получение пользователя
```bash
curl -X GET http://localhost:3000/api/users/USER_ID \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Использование на Vercel

Замените `http://localhost:3000` на ваш Vercel URL (например, `https://your-project.vercel.app`).
